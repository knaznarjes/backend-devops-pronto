apiVersion: v1
kind: Service
metadata:
  name: spring-backend
  namespace: monitoring
  labels:
    app: spring-backend
spec:
  type: ClusterIP
  ports:
    - port: 9090  # Correction du port
      targetPort: 9090  # Correction du port
      name: http
  selector:
    app: spring-backend